# CSS Selectors Assignment
This is my CSS selectors assignment.
In this project, I created a simple HTML page and connected it with an external CSS file.
I used different CSS selectors such as element selector, class selector and id selector.
I also used nth-child to change the color of list items.
The files used in this project are index.html and style.css.